{
  "scripts": {
    "build": "gulp build",
    "minify-css": "gulp minify-css",
    "uglify-js": "gulp uglify-js",
    "gzip": "gulp gzip",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-uglify": "^3.0.2",
    "gulp-gzip": "^1.4.2"
  }
}
